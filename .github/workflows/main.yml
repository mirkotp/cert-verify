# main.yml
name: CI
on:
  push:
    branches: [ main ]
jobs:
    container-job:
        runs-on: ubuntu-latest
        container:
            image: 'mirkotp/cert-verify'
            volumes:
                - ./CA:/CA
                - ./Untrusted:/Untrusted
        steps:
            - name: Check for dockerenv file
              run: sh /entry.sh
