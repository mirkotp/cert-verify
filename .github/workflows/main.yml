# main.yml
name: CI
on:
  push:
    branches: [ main ]
jobs:
    container-job:
        runs-on: ubuntu-latest
        container:
            image: 'mirkotp/cert-verify'
            volumes:
                - ${GITHUB_ACTION_PATH}/CA:/CA
                - ${GITHUB_ACTION_PATH}/Untrusted:/Untrusted
        steps:
            - name: VERIFY CERTIFICATES
              run: sh /entry.sh
            - name: VERIFY CERTIFICATES 2
              run: echo ${GITHUB_ACTION_PATH}